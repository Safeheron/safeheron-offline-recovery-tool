import { token } from './tokens'

import bg from '@img/bg.png'

export const brandName = 'Safeheron'

export const theme = {
  color: {
    brand: token('Theme/1'),
    primary: token('Theme/1'),
    hover: token('Theme/5'),
    bg: token('Theme/Color'),
  },
  img: {
    bg,
  },
}
